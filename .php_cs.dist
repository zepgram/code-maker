<?php

$finder = PhpCsFixer\Finder::create()->in(__DIR__);

$config = PhpCsFixer\Config::create()
    ->setRiskyAllowed(false)
    ->setRules([
        '@PSR1' => true,
        '@PSR2' => true,
    ])
    ->setFinder($finder);

return $config;
